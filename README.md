# ⚽ Game On API - PDF Generator

API para gerar PDFs personalizados. Feito em Express com Puppeteer e EJS.

# Como iniciar o servidor

Instalar as dependências
```
npm install
```

Rodar o servidor
```
node app.js
```
